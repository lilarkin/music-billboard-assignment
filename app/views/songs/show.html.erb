<h1> <%= @song.title %> </h1>

<ul>
  <li>Name: <%= @song.title %> </li>
  <li>
    <%= link_to 'Edit', edit_artist_song_path(@artist, @song) %>
    <%= link_to 'Delete', artist_song_path(@artist), method: :delete, data: {confirm: 'Really Delete?'} %>
  </li>
</ul>

<ul>
  <li>Name: <%= @song.album %> </li>
  <li>
    <%= link_to 'Edit', edit_artist_song_path(@artist, @song) %>
    <%= link_to 'Delete', artist_song_path(@artist), method: :delete, data: {confirm: 'Really Delete?'} %>
  </li>
</ul>

<%= link_to 'Home', artist_songs_path(@artist) %>